# Temporal island data queries using the get_island_temporal_data function
# Generated automatically for use in geosql.ttl

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "sansecondo_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_sansecondo_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "santospirito_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_santospirito_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "sanservolo_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_sanservolo_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "sangiorgioinalga_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_sangiorgioinalga_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "certosa_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_certosa_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "lazzarettovecchio_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_lazzarettovecchio_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "madonnadelmonte_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_madonnadelmonte_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "saccasessola_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_saccasessola_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "crevan_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_crevan_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "isoladeilaghi_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_isoladeilaghi_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "lido_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_lido_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "burano_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_burano_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "sanmichele_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_sanmichele_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "saccasanbiagio_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_saccasanbiagio_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "ottagonoabbandonato_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_ottagonoabbandonato_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "poveglia_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_poveglia_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "fisolo_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_fisolo_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "saccafisola_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_saccafisola_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "expoveglia_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_expoveglia_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "santacristina_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_santacristina_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "casonmontiron_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_casonmontiron_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "sanlazzarodegliarmeni_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_sanlazzarodegliarmeni_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "bueldellovo_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_bueldellovo_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "pellestrina_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_pellestrina_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "sangiacomoinpaludo_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_sangiacomoinpaludo_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "carbonera_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_carbonera_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "giudecca_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_giudecca_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "sanfrancescodeldeserto_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_sanfrancescodeldeserto_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "torcello_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_torcello_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "lacura_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_lacura_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "mazzorbo_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_mazzorbo_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "chioggia_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_chioggia_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "sanclemente_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_sanclemente_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "lazzarettonuovo_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_lazzarettonuovo_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "murano_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_murano_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "ottagonodeglialberoni_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_ottagonodeglialberoni_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "sangiuliano_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_sangiuliano_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "ottagonodisanpietro_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_ottagonodisanpietro_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "tessera_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_tessera_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
] ;

rs_sql_sail:includesSQLQuery
[
    rs_sql_sail:hasQueryId "legrazie_island_shapes_temporal" ;
    rs_sql_sail:text "SELECT DISTINCT          data.identifier::text as bw_id,         ST_AsText(ST_Transform(data.geometry, 3857))::text as wkt,         COALESCE(sy.start::text,              CASE                  WHEN col_data.key = 'today' THEN '2023'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as bob,         COALESCE(sy.end::text,             CASE                  WHEN col_data.key = 'today' THEN '2025'                 ELSE SUBSTRING(col_data.key FROM '^(\\d{4})')             END         )::text as eoe     FROM public.qgis_legrazie_islands data,     LATERAL jsonb_each_text(         to_jsonb(data) - 'identifier' - 'geometry' - 'identifier_short' - 'name'     ) as col_data     LEFT JOIN production.sources_years sy ON sy.source = col_data.key     WHERE col_data.value::boolean = true"
]  .
